service: () -> {
    getUser: (text) -> (variant {Ok:record {id:text; name:text; lastName:text}; Err:text}) query;
    getUsers: () -> (vec record {id:text; name:text; lastName:text}) query;
    register: (record {id:text; name:text; lastName:text}) -> (text);
}
